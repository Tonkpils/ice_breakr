%h1
  Your Chat Zone
%small
  Signed in as #{@user.username}
%ul.chat_list
  - @users.each do |user|
    - if user.id != @user.id
      %li= link_to user.username, private_chat_path(user_id: user.id)

= # Main Chat Box

= text_field_tag :message
= submit_tag :send

